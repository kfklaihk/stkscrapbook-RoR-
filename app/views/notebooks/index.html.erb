<p id="notice"><%= notice %></p>

<h1>Notebooks</h1>

<table>
  <thead>
    <tr>
      <th>Userid</th>
      <th>Stkcode</th>
      <th>Datme</th>
      <th>Category</th>
      <th>Fmt</th>
      <th>Fileurl</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @notebooks.each do |notebook| %>
      <tr>
        <td><%= notebook.userid %></td>
        <td><%= notebook.stkcode %></td>
        <td><%= notebook.datme %></td>
        <td><%= notebook.category %></td>
        <td><%= notebook.fmt %></td>
        <td><%= notebook.fileurl %></td>
        <td><%= link_to 'Show', notebook %></td>
        <td><%= link_to 'Edit', edit_notebook_path(notebook) %></td>
        <td><%= link_to 'Destroy', notebook, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Notebook', new_notebook_path %>
